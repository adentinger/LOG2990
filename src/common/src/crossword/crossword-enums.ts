/**
 * Grid Word
 */
export enum Direction {
    horizontal,
    vertical,
}

export enum Owner {
    none,
    player1,
    player2,
}

/**
 * Game Configs
 */
export enum Difficulty {
    easy,
    medium,
    hard,
}

export enum GameMode {
    Classic,
    Dynamic,
}